# 5. Local Persistence with AsyncStorage

We decided to use `@react-native-async-storage/async-storage` to store and persist custom timers locally on the device. This provides a lightweight key-value store that is easy to integrate, requires no schema migrations for simple JSON structures, and supports offline operation.
