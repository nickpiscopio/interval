# 8. Import Preview Screen

We decided that incoming deep links containing shared timers will route to the `CreateTimerScreen` in a read/preview state. This allows the user to inspect the details, customize the intervals, and choose to save or run the workout, preventing unwanted clutter in their local storage.
