# Upgrade to Expo SDK 57 and React Navigation v7

We decided to upgrade the project to Expo SDK 57 and React Navigation v7, and to remove the unused `react-native-sound` dependency. This resolves all 93 security vulnerabilities, ensures compatibility with React 19.2 / React Native 0.86, and removes obsolete native modules to simplify the build process.
